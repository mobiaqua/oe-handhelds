
Usage Information:

	Run ". setup" to setup OE environment.

	Setup generate/load configuration files:
	- build-${DISTRO}/conf/local.conf
	- build-${DISTRO}/env.source
	- build-${DISTRO}/crosstools-setup

	Then use bitbake command.

